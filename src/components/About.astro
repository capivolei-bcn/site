---
import { UsersIcon, SunIcon, HeartHandshakeIcon } from "@lucide/astro";

const content = {
  title: "Sobre Nós",
  description: [
    "Somos uma comunidade vibrante de entusiastas do vôlei em Barcelona, dedicados a promover o esporte e criar conexões significativas entre nossos membros.",
    "Organizamos jogos regulares, eventos sociais e treinamentos para todos os níveis de habilidade, desde iniciantes até jogadores experientes.",
    "Nosso objetivo é fomentar um ambiente inclusivo e acolhedor onde todos possam desfrutar do vôlei, fazer novos amigos e se manter ativos.",
  ],
};
---

<section class="py-24 bg-accent-400 border-y-2 border-slate-900">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div
      class="flex flex-col md:flex-row md:items-end justify-between mb-16 gap-6"
    >
      <div>
        <h2
          class="font-display text-5xl font-bold text-slate-900 tracking-tight leading-none"
        >
          {content.title}
        </h2>
        <div class="h-2 w-24 bg-slate-900 mt-4"></div>
      </div>
      <div class="hidden md:block">
        <span class="text-4xl" role="img" aria-label="Volleyball">🏐</span>
      </div>
    </div>

    <div class="grid md:grid-cols-3 gap-6">
      {
        content.description.map((text, index) => (
          <div
            class={`
                relative p-8 rounded-xl border-2 border-slate-900 shadow-hard transition-transform duration-300 hover:-translate-y-1
                ${
                  [
                    "bg-white text-slate-900",
                    "bg-brand-100 text-slate-900",
                    "bg-white text-slate-900",
                  ][index % 3]
                }
              `}
          >
            <div class="w-14 h-14 bg-white border-2 border-slate-900 rounded-lg flex items-center justify-center mb-6 shadow-[2px_2px_0px_0px_rgba(0,0,0,1)]">
              {
                [
                  <UsersIcon class="w-8 h-8 text-slate-900" />,
                  <SunIcon class="w-8 h-8 text-slate-900" />,
                  <HeartHandshakeIcon class="w-8 h-8 text-slate-900" />,
                ][index % 3]
              }
            </div>

            <p class="text-slate-800 font-medium text-lg leading-relaxed">
              {text}
            </p>
          </div>
        ))
      }
    </div>
  </div>
</section>
