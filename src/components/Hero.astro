---
import { ArrowRightIcon } from "@lucide/astro";
const content = {
  title: "Junte-se à Comunidade de Vôlei de Barcelona",
  subtitle:
    "Conecte-se com entusiastas do vôlei, participe de jogos emocionantes e faça parte de uma comunidade acolhedora em Barcelona.",
  cta: "Participe Agora",
};
---

<section
  class="relative min-h-screen flex items-center pt-24 pb-12 overflow-hidden bg-[#f8fafc]"
>
  {/* Abstract Background Pattern */}
  <div
    class="absolute inset-0 opacity-[0.03]"
    style={{
      backgroundImage: "radial-gradient(#1e293b 1px, transparent 1px)",
      backgroundSize: "24px 24px",
    }}
  >
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 w-full">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-24">
      {/* Text Content */}
      <div class="flex-1 text-center lg:text-left order-2 lg:order-1">
        <h1
          class="font-display font-bold text-5xl sm:text-6xl lg:text-7xl text-slate-900 leading-[1] mb-6 tracking-tight"
        >
          {content.title}
        </h1>

        <p
          class="text-lg sm:text-xl text-slate-600 mb-8 leading-relaxed max-w-2xl mx-auto lg:mx-0 font-medium"
        >
          {content.subtitle}
        </p>

        <div
          class="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-4"
        >
          <button
            class="group relative inline-flex items-center justify-center px-8 py-4 text-lg font-bold text-slate-900 transition-all duration-200 bg-brand-500 border-2 border-slate-900 rounded-xl shadow-hard hover:translate-x-[2px] hover:translate-y-[2px] hover:shadow-none active:bg-brand-600"
          >
            <span>{content.cta}</span>
            <ArrowRightIcon
              class="ml-2 w-6 h-6 transition-transform group-hover:translate-x-1"
            />
          </button>
        </div>
      </div>

      {/* Visual Content */}
      <div
        class="flex-1 w-full max-w-lg lg:max-w-xl order-1 lg:order-2 relative"
      >
        {/* Decorative Elements */}
        <div
          class="absolute -top-6 -right-6 w-16 h-16 bg-accent-400 rounded-full border-2 border-slate-900 z-20 flex items-center justify-center shadow-hard-sm animate-float"
        >
          <span class="text-2xl">🇧🇷</span>
        </div>
        <div
          class="absolute -bottom-4 -left-4 w-12 h-12 bg-blue-400 rounded-lg border-2 border-slate-900 z-20 flex items-center justify-center shadow-hard-sm animate-float"
          style={{ animationDelay: "1.5s" }}
        >
          <span class="text-xl">🇪🇸</span>
        </div>

        {/* Main Image Frame */}
        <div
          class="relative rounded-2xl border-2 border-slate-900 bg-white p-2 shadow-hard-lg transform rotate-2 transition-transform hover:rotate-0 duration-300"
        >
          <div class="rounded-xl overflow-hidden bg-slate-200 aspect-[4/3]">
            <img
              src="https://images.unsplash.com/photo-1612872087720-bb876e2e67d1?q=80&w=1607&auto=format&fit=crop"
              alt="Indoor Volleyball"
              class="w-full h-full object-cover"
            />
          </div>

          {/* Caption Tag */}
          <div
            class="absolute bottom-6 right-6 bg-white px-4 py-2 rounded-lg border-2 border-slate-900 shadow-hard-sm"
          >
            <span class="font-bold font-display text-sm">Capybara Club</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
